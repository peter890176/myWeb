[phases.setup]
aptPkgs = ["default-libmysqlclient-dev", "build-essential"]

[phases.install]
cmds = [
  "python -m venv /opt/venv",
  ". /opt/venv/bin/activate",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = [
  "cd backend",
  "python manage.py collectstatic --noinput"
]

[start]
cmd = "gunicorn api.wsgi --chdir backend"
